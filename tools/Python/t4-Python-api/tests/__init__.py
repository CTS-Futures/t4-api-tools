"""Test suite for the t4login package.

Mirrors the source tree structure. Tests are split into:
- Unit tests (default): run without network/credentials.
- Integration tests (marked ``@pytest.mark.integration``): hit the live sim API.
"""
