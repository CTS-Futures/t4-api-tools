"""Domain definitions for the T4 trading platform.

Contains enums, value types, and data structures that model the T4 trading
domain, including:

- :class:`~t4login.definitions.bid_offer.BidOffer` — trade aggressor side enum.
- :class:`~t4login.definitions.market_mode.MarketMode` — market session state enum.
- ``chartdata`` sub-package — binary stream readers and chart state objects.
- ``priceconversion`` sub-package — price arithmetic and variable tick tables.
"""
