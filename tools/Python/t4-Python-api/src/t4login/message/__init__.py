"""Binary message readers for the T4 wire format.

Provides module-level functions for reading typed values (integers, strings,
datetimes, prices) from a binary stream using the .NET BinaryWriter-compatible
encoding that the T4 API uses on the wire.
"""
